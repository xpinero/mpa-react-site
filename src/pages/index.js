import React from 'react';

const landing = require('../images/landing.jpg');

const Home = () => {
return (
	<div>
	<imageBackground source={landing}>
		<h1>Welcome to my Real-estate page!</h1>
	</imageBackground>
	
	</div>
);
};

export default Home;
